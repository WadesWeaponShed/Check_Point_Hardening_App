{
  "name": "check-point-hardening-app-se",
  "version": "0.1.0",
  "private": true,
  "description": "Single-use SE hardening scanner for Check Point environments.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "install:browsers": "playwright install chromium"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "playwright": "^1.54.1"
  }
}
